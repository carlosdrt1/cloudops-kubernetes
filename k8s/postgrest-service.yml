apiVersion: v1
kind: Service
metadata:
  name: postgrest-service
  namespace: cloudops-kubernetes
spec:
  type: NodePort
  selector:
    app: postgrest
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001
